{
  "name": "cys_storefront_kit",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "dev": "next dev -p 55500",
    "build": "next build",
    "predeploy": "rm -rf out && npm run build",
    "deploy": "gh-pages -d out -b gh-pages"
  },
  "dependencies": {
    "next": "^16.0.1",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "gh-pages": "^6.1.1"
  }
}
